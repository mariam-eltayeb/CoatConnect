// featured organizer slider
$('.search_ads').slick({
    centerMode: true,
    centerPadding: '0px',
    slidesToShow: 1,
    slidesToscroll: 1,
    autoplay: true,
    autoplaySpeed: 2000,
    arrows: false,
    dots: false,
    centerMode: true,
    focusOnSelect: true
  });
  // createSlick();
  // $(window).on( 'resize', createSlick );
